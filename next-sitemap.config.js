module.exports = {
    siteUrl: 'https://coneixement-assignment-frontend.vercel.app', // Update to production URL
    generateRobotsTxt: true, // (optional)
    sitemapSize: 7000,
    outDir: './public', // Optional: the directory for your sitemap file
    changefreq: 'daily', // Optional: set the frequency of changes for search engines
    priority: 0.7, // Optional: priority for the pages
};

// module.exports = {
//     siteUrl: process.env.NEXT_PUBLIC_API_URL || 'https://coneixement-assignment-frontend.vercel.app/', // Update this
//     generateRobotsTxt: true, // Ensure robots.txt is being generated
// };
